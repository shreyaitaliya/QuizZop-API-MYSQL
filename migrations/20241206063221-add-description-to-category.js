'use strict';

// Add Column
// module.exports = {
//   async up(queryInterface, Sequelize) {
//     await queryInterface.addColumn('category', 'description', {
//       type: Sequelize.STRING(500), // Define the data type and size of the column
//       allowNull: true, // Allow the field to be nullable
//     });
//   },

//   async down(queryInterface, Sequelize) {
//     await queryInterface.removeColumn('category', 'description');
//   }
// }     